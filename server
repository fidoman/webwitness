Data at server:
1. List of registered clusters
  Name+id, key
  When cluster is registered, file with information about it must be created with racingless method

2. List of nodes per cluster
  Create files for each node:
    node#key
    node#alive
    node#status

3. Additional data per node/cluster
  Cluster config:
    keep-alive period
    keep-alive timeout
    nodes list
    witnesses list
    config version
    secondary admin e-mail
  Per-node config:
    online services list
    used config version
  Per-service config:
    service-specific data


Data are stored at witnesses, some nodes may not to have witnesses installed.
